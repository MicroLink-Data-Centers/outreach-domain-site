{
  "name": "microlinkdc-site",
  "private": true,
  "scripts": {
    "deploy": "set \"NODE_OPTIONS=--dns-result-order=ipv4first\" && npx wrangler pages deploy . --project-name microlinkdc-site --branch=main --commit-dirty=true",
    "predeploy": "node tools/predeploy-guard.cjs"
  }
}
